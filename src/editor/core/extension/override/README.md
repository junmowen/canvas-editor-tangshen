# Extension Override 目录索引

`override/` 存放默认行为覆盖运行对象。

## 位置说明

- 所属层级：默认行为覆盖层

## 文件说明

| 文件 | 职责 |
| --- | --- |
| `Override.ts` | copy / paste / drop / menu 等默认行为覆盖入口 |

## 函数说明

| 文件 | 函数 / 方法 | 作用 | 调用位置 |
| --- | --- | --- | --- |
| `Override.ts` | 覆盖回调注册和读取方法 | 提供外部覆盖默认行为的入口。 | `Draw` 和 DOM 事件链路 |

