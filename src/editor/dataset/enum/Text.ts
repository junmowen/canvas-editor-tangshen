/** 文字装饰线样式，控制下划线、删除线等线型。 */
export enum TextDecorationStyle {
  SOLID = 'solid',
  DOUBLE = 'double',
  DASHED = 'dashed',
  DOTTED = 'dotted',
  WAVY = 'wavy'
}

/** 线条虚线样式，控制边框或装饰线的实线、虚线和点线形态。 */
export enum DashType {
  SOLID = 'solid',
  DASHED = 'dashed',
  DOTTED = 'dotted'
}
