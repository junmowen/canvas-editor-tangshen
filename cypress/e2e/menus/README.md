# Cypress Menus E2E 目录索引

这个目录存放 demo 工具栏菜单和通用用户入口用例。

表格专项已移动到 `cypress/e2e/table/`，这里保留非表格菜单，例如文本、图片、公式、搜索、水印、打印和分页符等。

## 命名规则

- 文件名对应菜单或交互入口，例如 `image.cy.ts`、`search.cy.ts`。
- 跨功能域的复杂回归优先放入 `issues/` 或对应专题目录。
